# Frontend Configuration for Survey App

# API Settings
API_PROTOCOL=http
API_HOST=localhost
API_PORT=8080
API_VERSION=v1

# Frontend Settings
FRONTEND_PROTOCOL=http
FRONTEND_HOST=localhost
FRONTEND_PORT=3000

