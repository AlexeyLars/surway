# Frontend Environment Variables
# Copy this file to .env.local and adjust values as needed
# These values correspond to configs/frontend.conf

# API Configuration
NEXT_PUBLIC_API_PROTOCOL=http
NEXT_PUBLIC_API_HOST=localhost
NEXT_PUBLIC_API_PORT=8080
NEXT_PUBLIC_API_VERSION=v1

# Frontend Configuration
NEXT_PUBLIC_FRONTEND_PROTOCOL=http
NEXT_PUBLIC_FRONTEND_HOST=localhost
NEXT_PUBLIC_FRONTEND_PORT=3000

# Note: On Vercel deployments, VERCEL_URL is automatically set
# and will override FRONTEND_* variables for the base URL
